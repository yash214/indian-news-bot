"""Future home for News Agent routes.

TODO: Move `/api/news/agent/*` routes out of `backend.app` once the app is
converted to blueprints. Keeping them in `app.py` for now avoids changing
request wiring during this structure-only refactor.
"""

